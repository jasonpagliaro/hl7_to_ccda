{
  "name": "hl7-to-ccda",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "DOTNET_ROOT=$HOME/.dotnet PATH=$HOME/.dotnet:$PATH dotnet run --project src/Hl7ToCcda.Api/Hl7ToCcda.Api.csproj --urls http://localhost:5080",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "test:web": "npm run test --workspace web",
    "lint:web": "npm run lint --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
